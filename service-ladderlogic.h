#ifndef SERVICE_LADDERLOGIC_H
#define SERVICE_LADDERLOGIC_H

// use a static function and an array of sensor function pointers, conditions, and result function pointers and parameters
// iterate through the array, evaluating.  One entry in the array pushed or popped per command

// can return errors of all priorities

#endif // SERVICE_LADDERLOGIC_H
